# ClubLog HA Bridge - Environment Configuration
# Copy this file to .env and fill in your values

# ==============================================================================
# ClubLog API Credentials (REQUIRED)
# ==============================================================================
# Request an API key at https://clublog.org/need_api.php
CLUBLOG_API_KEY=YOUR_API_KEY_HERE
# Your ClubLog account email
CLUBLOG_EMAIL=your@email.com
# Application Password — generate in ClubLog Settings → Application Passwords
# This is NOT your main login password. ClubLog requires app-specific passwords
# for third-party integrations.
CLUBLOG_APP_PASSWORD=YOUR_APP_PASSWORD_HERE

# ==============================================================================
# Core Identity (REQUIRED)
# ==============================================================================
# Your amateur radio callsign
MY_CALLSIGN=YOUR_CALLSIGN

# ==============================================================================
# Home Assistant MQTT Broker (REQUIRED for Docker mode)
# ==============================================================================
HA_MQTT_BROKER=YOUR_MQTT_BROKER_IP
HA_MQTT_PORT=1883
HA_MQTT_USER=
HA_MQTT_PASS=

# ==============================================================================
# Debugging
# ==============================================================================
DEBUG_MODE=False

# ==============================================================================
# Poll Intervals (seconds)
# ==============================================================================
# DXCC matrix (default: 3600 = 60 min, server caches for 60 min)
MATRIX_INTERVAL=3600
# Watch/monitor data (default: 600 = 10 min)
WATCH_INTERVAL=600
# Most wanted rankings (default: 604800 = weekly, per ClubLog maintainer)
MOST_WANTED_INTERVAL=604800
# Band activity data (default: 86400 = daily, updated only a few times per year)
ACTIVITY_INTERVAL=86400
# Active expeditions (default: 3600 = 60 min)
EXPEDITIONS_INTERVAL=3600
# Active livestreams (default: 600 = 10 min)
LIVESTREAMS_INTERVAL=600

# ==============================================================================
# Cross-Project Integration
# ==============================================================================
# Path to export wanted list file (consumed by pskr-ha-bridge / wspr-ha-bridge)
# WANTED_LIST_EXPORT_PATH=/data/clublog_wanted.txt

# ==============================================================================
# Home Assistant Discovery
# ==============================================================================
HA_DISCOVERY_PREFIX=homeassistant
HA_ENTITY_BASE=clublog
